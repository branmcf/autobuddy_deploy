import { FilterCompletedRepairsPipe } from './filter-completed-repairs.pipe';

describe('FilterCompletedRepairsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompletedRepairsPipe();
    expect(pipe).toBeTruthy();
  });
});
